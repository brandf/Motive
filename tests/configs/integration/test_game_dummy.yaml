# Test game configuration with dummy LLM provider for end-to-end tests
# This prevents real API calls during testing

includes:
  - "themes/fantasy/editions/hearth_and_shadow/hearth_and_shadow.yaml"

# Game settings
game_settings:
  num_rounds: 1
  manual: "../docs/MANUAL.md"
  initial_ap_per_turn: 30
  log_path: "fantasy/hearth_and_shadow/{game_id}"

# Test players with dummy provider
players:
  - name: "TestPlayer1"
    provider: "dummy"
    model: "test"

  - name: "TestPlayer2"
    provider: "dummy"
    model: "test"
